<?xml version="1.0" encoding="UTF-8"?>
<topsApp>
  <component name="topsinsar">
    <component name="reference">
      <property name="orbit directory">{{  orbit_directory  }}</property>
      <property name="auxiliary data directory">aux_cal</property>
      <property name="output directory">{{ output_reference_directory }}</property>
      <property name="safe">{{  ref_zip_file  }}</property>
    </component>
    <component name="secondary">
      <property name="orbit directory">{{ orbit_directory }}</property>
      <property name="auxiliary data directory">aux_cal</property>
      <property name="output directory">{{ output_secondary_directory }}</property>
      <property name="safe">{{ sec_zip_file }}</property>
    </component>
    <property name="swaths">{{ swaths }}</property>
    <property name="range looks">{{ range_looks }}</property>
    <property name="azimuth looks">{{ azimuth_looks }}</property>
    <property name="filter strength">{{ filter_strength }}</property>
    <property name="region of interest">{{ region_of_interest }}</property>
    <property name="demFilename">{{ demFilename }}</property>
    <property name="geocodeDemFilename">{{ geocodeDemFilename }}</property>
    <property name="do unwrap">True</property>
    <property name="unwrapper name">snaphu_mcf</property>
    <property name="do ionosphere correction">{{ estimate_ionosphere_delay }}</property>
    <property name="consider burst properties in ionosphere computation">{{ ion_burstProperties }}</property>
    <property name="start ionosphere step">{{ ion_startStep }}</property>
    <property name="end ionosphere step">{{ ion_stopStep }}</property>
    <property name="apply polynomial fit before filtering ionosphere phase">{{ ion_polyFit }}</property>
    <property name="correct phase error caused by ionosphere azimuth shift">{{ ion_correctAzimuthshift }}</property>
    <property name="apply ionosphere correction">False</property>
    <property name="do ESD">{{ do_esd }}</property>
    <property name="ESD coherence threshold">{{ esd_coherence_threshold }}</property>
    <property name="use virtual files">{{ use_virtual_files }}</property>
    <property name="geocode list">{{ geocode_list }}</property>
  </component>
</topsApp>